"use client";

export async function getBackgroundImage(text: string) {
  const image = await new Promise<void>((resolve) => {
    
  });
  await new Promise((resolve) => setTimeout(resolve, 500));
  return image;
}
